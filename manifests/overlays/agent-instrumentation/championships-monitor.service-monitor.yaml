apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: championships-monitor
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: championships
  endpoints:
    - port: metrics
      interval: 30s
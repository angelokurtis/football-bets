apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: teams-monitor
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: teams
  endpoints:
    - port: metrics
      interval: 30s
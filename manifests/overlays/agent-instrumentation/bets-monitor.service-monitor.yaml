---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bets-monitor
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: bets
  endpoints:
    - port: metrics
      interval: 30s